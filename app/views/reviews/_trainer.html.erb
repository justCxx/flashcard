<%= simple_form_for [:review, card], as: :review,
  url: :review_card, method: :put do |f| %>
  <%= f.input :id, as: :hidden %>
  <%= f.input :translated_text, as: :string %>
  <%= text_field_tag :answer %>
  <%= f.button :submit, "Проверить" %>
<% end %>
